import { FirstLetterColorDirective } from './first-letter-color.directive';

describe('FirstLetterColorDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstLetterColorDirective();
    expect(directive).toBeTruthy();
  });
});
