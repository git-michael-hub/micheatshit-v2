import { BypassStringIndexPipe } from './bypass-string-index.pipe';

describe('BypassStringIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new BypassStringIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
